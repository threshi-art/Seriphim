[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seraphim-runtime-foundation"
version = "0.1.0"
description = "Bounded local Runtime storage and evidence-migration foundation for Seraphim G1-02"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["seraphim_runtime"]
