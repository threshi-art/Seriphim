# Curated import provenance

Imported on 2026-08-05 from the local EI-RAM API workspace.

## Included

- Python application and desktop-shell source
- FastAPI entry points and schemas
- Deterministic scoring modules and services
- Safe sample input
- Requirements files
- Two source tests

## Excluded

- `build/` and `dist/` packaging output
- Logs, caches, bytecode, and virtual environments
- PyInstaller spec iterations
- Backup copies of the source tree
- Private research material outside the API workspace

This import records the source as it existed during portfolio curation. It does
not reconstruct earlier commits or claim that 2026-08-05 was the project's
original start date.
